[project]
name = "data-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Deepakreddy1510", email = "deepakreddy1510@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "langchain>=1.3.16",
    "langchain-google-genai>=4.3.5",
    "langchain-groq>=1.1.3",
    "langgraph>=1.2.11",
    "pydantic>=2.13.4",
]

[project.scripts]
data-agent = "data_agent:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
